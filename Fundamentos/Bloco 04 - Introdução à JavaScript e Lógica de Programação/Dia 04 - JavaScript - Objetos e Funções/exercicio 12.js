let nomes = ['José', 'Lucas', 'Nádia', 'Fernanda', 'Cairo', 'Joana'];

function maiorNome (nomes) {
    let storage = [];
    for (let i = 0; i < nomes.length; i += 1) {
        storage.push(nomes[i].split(''));
    }
    return storage;
}
for (let i = 0; i < storage.length; i += 1) {
    if (storage[i] < 
}
console.log(maiorNome(nomes));